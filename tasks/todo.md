# Active Task Plan

## Goal

Enable English as a second application language, ship the work as `v2.1.6`, and keep the GitHub updater path compatible for users moving from `v2.1.5`.

## Execution Plan

- [ ] Confirm the real UI text surfaces, startup entry points, and updater/release version contract
- [ ] Add a central language service with persisted `tr` / `en` selection
- [ ] Wire the main window and key panels/dialogs to re-apply translated UI text safely
- [ ] Bump version metadata and write `v2.1.6` release notes/update docs
- [ ] Run targeted verification for syntax, localization switching, updater metadata, and release packaging
- [ ] Build the Windows release artifacts and publish the GitHub release

## Review Notes

- Pending
